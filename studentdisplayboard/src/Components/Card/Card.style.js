import { styled, Typography as Text, Card } from "@mui/material";

export const MUICard = styled(Card)(() => ({
  width: 300,
  color: "#FE0057",
  backgroundColor: "#42a5f5",
}));

export const Typography = styled(Text)(() => ({
  color: "#DDFFE7",
}));
